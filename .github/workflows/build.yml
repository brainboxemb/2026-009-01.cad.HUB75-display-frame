name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "dsg/**"
      - "project.yml"
      - ".gitmodules"
      - "tools/tool.scad-project"
      - ".github/workflows/build.yml"
  pull_request:
    paths:
      - "dsg/**"
      - "project.yml"
      - ".gitmodules"
      - "tools/tool.scad-project"
      - ".github/workflows/build.yml"

permissions:
  contents: write
  packages: read

jobs:
  build:
    # Keep only the newest run for this workflow and Git ref.
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true
    uses: brainboxemb/tool.scad-project/.github/workflows/project-build.yml@ba93e9165f8c23df3f867c2ae7fa160187166d9d
    with:
      artifact_name: hub75-display-frame-build
