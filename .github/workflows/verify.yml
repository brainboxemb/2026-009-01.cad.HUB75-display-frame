name: Verify

on:
  workflow_dispatch:
  push:
    branches:
      - "**"
      - "!release-request/**"
    paths:
      - "dsg/**"
      - "vrf/**"
      - "scripts/**"
      - "project.yml"
      - ".gitmodules"
      - "tools/tool.scad-project"
      - ".github/workflows/verify.yml"
  pull_request:
    paths:
      - "dsg/**"
      - "vrf/**"
      - "scripts/**"
      - "project.yml"
      - ".gitmodules"
      - "tools/tool.scad-project"
      - ".github/workflows/verify.yml"

permissions:
  contents: write
  packages: read

jobs:
  verify:
    # Keep only the newest run for this workflow and Git ref.
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true
    uses: brainboxemb/tool.scad-project/.github/workflows/project-verify.yml@0f1f6c8576f0b82544124db8f13a62f0a859946f
