name: Verify HUB75 display frame

on:
  workflow_dispatch:
  push:
    branches:
      - "**"
    tags:
      - "v*"
  pull_request:

permissions:
  contents: write
  packages: read

jobs:
  verify:
    uses: brainboxemb/tool.scad-project/.github/workflows/project-verify.yml@v0.7.0
